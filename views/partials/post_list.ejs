<% if (posts.length === 0) { %>
    <p>No posts yet.</p>
<% } else { %>
    <% for(post of posts) { %> 
        <div class="card white_shadow">
            <div class="top">
                <img src="<%- post.user.picture_path -%>" alt="">
                <label><%- post.user.firstname -%> <%- post.user.lastname -%></label>
                <!-- <button class="icon" id="options">
                    <i class="fa-solid fa-ellipsis"></i>
                </button> -->
            </div>
            <hr>
            <div class="middle">
                <div class="image">
                    <img class="white_shadow" src="<%= post.data_path %>" alt="image"/>
                </div>
                <div class="description">
                    <label id="description" name="" id="" cols="30" rows="10"><%= post.description %></label>
                </div>
            </div>
            <hr>
            <div class="bottom">
                <% if( post.liked ){ %>
                    <a href="/feed/dislike/<%= post.id_post %>"> 
                        <button if="" class="icon" id="like">
                            <i class="fa-solid fa-heart" >
                                <label for=""><%- post.nbLike -%></label>
                            </i>
                        </button>
                    </a>
                <% } else{ %>  
                    <a href="/feed/like/<%= post.id_post %>">
                        <button class="icon" id="like">
                            <i class="fa-regular fa-heart" >
                                <label for=""><%- post.nbLike -%></label>
                            </i>
                        </button>
                    </a>
                 <% } %>
                <button class="icon" id="comment">
                    <i class="fa-regular fa-comment">
                        <label for="">15</label>
                    </i>
                <!-- </button>
                <button class="icon" id="send">
                    <i class="fa-regular fa-paper-plane"></i>
                </button> -->
                <a href="/save/<%- post.id_post %>" class="save-button">
                    <button class="icon" id="save">
                        <% if(post.isSaved === true) { %>
                            <i class="fa-solid fa-bookmark"></i>
                        <% } else { %>
                            <i class="fa-regular fa-bookmark"></i>
                        <% } %>
                    </button>
                </a>
            </div>
        </div>
    <% } %>
<% } %>
<script src="/js/save.js"></script>
